Rug option (as opposed to partial residuals)
Allow extrapolation
Allow multiple 'by' arguments in a list
From reviewers:
  * Review 'ask' settings
  * Check two-way ANOVA example with empty cells
Update help files
